// frontend/postcss.config.js (Verify or create this file if it's missing)
export default {
    plugins: {
        // Use the new PostCSS adapter package for Tailwind
        '@tailwindcss/postcss': {},
        autoprefixer: {},
    },
}